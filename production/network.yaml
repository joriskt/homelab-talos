# cluster:
  # etcd:
  #   advertisedSubnets:
  #     - 192.168.11.0/24

machine:
  kubelet:
    nodeIP:
      validSubnets:
        - 192.168.12.0/24
  network:
    interfaces:
      # Internet only interface
      - interface: ens18
        dhcp: true
