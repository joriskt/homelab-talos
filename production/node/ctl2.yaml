machine:
  network:
    hostname: control-2
    interfaces:
      # Cluster only interface + VIP
      - interface: ens19
        dhcp: false
        addresses:
          - 192.168.12.52/24
        routes:
          - network: 192.168.12.0/24
        vip:
          ip: 192.168.12.50
      # Storage only interface
      - interface: ens20
        dhcp: false
        addresses:
          - 192.168.3.72/24
        routes:
          - network: 192.168.3.0/24
  nodeLabels:
    topology.kubernetes.io/region: eu-west-0
    topology.kubernetes.io/zone: home
    topology.joris.me/node: nuc3
